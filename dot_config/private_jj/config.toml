[user]
name = "Ray Suliteanu"
email = "raysuliteanu@pm.me"

[ui]
default-command = "log"
diff.tool = ["delta", "--navigate", "--line-numbers", "--hyperlinks", "--color-only", "--side-by-side", "$left", "$right"]
